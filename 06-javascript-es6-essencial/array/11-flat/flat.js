// flat() - retorna um novo array com todos os elementos 
// de um sub-array concatenados de forma recursiva de acordo
// com a profundidade específicada(depth)
const arr = [1, 2, [3, 4]];

console.log(arr.flat());

