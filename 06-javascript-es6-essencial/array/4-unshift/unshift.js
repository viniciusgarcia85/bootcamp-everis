// unshift() - adiciona um ou mais elementos no início do array
const arr = ['banana', 'melancia', 'abacate'];
const addItem = arr.unshift('laranja')

// Retorna o tamanho atual do array
console.log(addItem)

// Retorna o array atual
console.log(arr)