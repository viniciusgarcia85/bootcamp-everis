// push() - adiciona um ou mais elementos no final do array e retorna o tamanho do novo array
const arr = ['banana', 'melancia', 'abacate'];
const arrLength = arr.push('acerola');

// Retorna o tamanho atual do array
console.log(arrLength)
// Exibe com o elemento inserido ao final do array
console.log(arr);